# Core LangChain 1.0 ecosystem (GA Oct 2025, stable through 2.0)
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-community>=0.4
langchain-text-splitters>=1.0,<2.0
langchain-chroma>=0.2

# Orchestration
langgraph>=1.0,<2.0
langgraph-checkpoint-sqlite>=2.0

# Vector store
chromadb>=0.5

# Observability (Phase 6)
langsmith>=0.8,<1.0

# Retrieval extras (Phase 3+)
rank-bm25>=0.2.2          # sparse / hybrid search
tavily-python>=0.5         # web search fallback (Phase 5)

# Document loaders
pypdf>=5.0               # PDF loading
unstructured>=0.16       # richer doc parsing (optional)

# Evaluation (Phase 2)
ragas>=0.2,<0.3               # pin to 0.2.x; 0.4+ has broken langchain imports
langchain-google-vertexai>=2.0 # required by ragas for vertexai compat shim
datasets>=3.0

# App / infra
python-dotenv>=1.0
pydantic>=2.0
fastapi>=0.115           # Phase 7
uvicorn>=0.32            # Phase 7
slowapi>=0.1.9           # Rate limiting (Phase 7)
streamlit>=1.40          # Chat UI (Phase 7)

# Testing
pytest>=8.0
pytest-cov>=5.0
